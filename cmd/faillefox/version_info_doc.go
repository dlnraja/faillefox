// Code généré par build.go à partir de version_info.json. Ne pas éditer à la main.
// Ce fichier définit la ressource VERSIONINFO Windows qui sera embarquée
// dans faillefox.exe via go:linkname lors de la compilation Windows.
// Les métadonnées (Company, Product, Version) rendent l'exécutable
// identifiable et réduisent les faux positifs des antivirus heuristiques.

package main

// Note : la vraie ressource .syso est générée par le workflow de build via
// `goversioninfo` ou rsrc. Ici on documente juste la configuration attendue.
// Le fichier version_info.json (racine du repo) contient les valeurs réelles.
